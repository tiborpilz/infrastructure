apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Forgejo blueprints get picked up by applications/authentik's aggregator.
# When the forgejo Helm chart deployment is migrated out of TF, list the
# Argo Application (or rendered manifests) under `resources:`.
resources: []
