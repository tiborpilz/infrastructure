apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: smtp
  namespace: argocd
spec:
  project: platform
  source:
    path: applications/services/smtp
    repoURL: https://github.com/tiborpilz/infrastructure
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: smtp
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=false
