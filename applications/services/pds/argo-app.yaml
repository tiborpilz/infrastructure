apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pds
  namespace: argocd
spec:
  project: platform
  source:
    path: applications/services/pds
    repoURL: https://github.com/tiborpilz/infrastructure
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: pds
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=false
