apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: snapshot-scheduler
  namespace: argocd
spec:
  project: platform
  source:
    path: applications/operators/snapshot-scheduler/manifests
    repoURL: https://github.com/tiborpilz/infrastructure
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: snapshot-scheduler
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=false
