apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gateway-dns
  namespace: argocd
spec:
  project: platform
  source:
    path: applications/operators/gateway-dns/manifests
    repoURL: https://github.com/tiborpilz/infrastructure
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: gateway-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=false
