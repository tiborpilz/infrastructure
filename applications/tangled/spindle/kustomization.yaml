apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Spindle (CI runner) resources. The shared tangled namespace is owned by the
# parent (applications/tangled). argo-app.yaml (the child Application) points its
# source here, so it is not listed.

resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - httproute.yaml
