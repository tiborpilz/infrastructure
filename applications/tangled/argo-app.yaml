apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tangled
  namespace: argocd
spec:
  project: platform
  source:
    path: applications/tangled
    repoURL: https://github.com/tiborpilz/infrastructure
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: tangled
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=false
